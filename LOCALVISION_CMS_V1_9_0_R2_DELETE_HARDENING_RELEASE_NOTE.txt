LocalVision CMS v1.9.0 R2 DELETE HARDENING

- v1.8.9의 right target UI/visibility 버그 수정 유지
- 콘텐츠 삭제 시 deleteFile=1인데도 R2 파일이 남는 문제 보강
- 같은 r2_key를 가진 r2_ 자동스캔 미러 행은 실제 참조로 보지 않고 R2 삭제를 막지 않음
- R2 파일 삭제 성공 시 같은 파일을 가리키는 r2_ 미러 D1 행도 함께 삭제
- 다른 실제 CMS 콘텐츠 행(ct_ 등)이 같은 파일을 공유할 때만 R2 삭제 보류
- 삭제 응답에 r2Deleted, r2DeleteSkipped, refCheck, d1MirrorRowsDeleted를 반환해 원인 확인 가능

APP/Player/R2/D1 초기화 없음. CMS 코드만 교체.
