LocalVision CMS v1.7.4 APP v9.1 호환 보강

추가:
- /api/native-command?id=lv001&store=...&deviceId=...

목적:
- APP v9.1이 Native 전용 명령(screenshot, hard_refresh, clear_cache, reload_app, reload_player)을 먼저 확인할 수 있도록 함.
- 기존 /api/app-config, /api/player-config, /api/devices, /api/screenshots는 유지.

R2 스크린샷 저장:
- APP이 /api/screenshots로 PNG를 업로드하면 CMS가 R2에 자동 저장.
- 경로: system/screenshots/<store>/<deviceId>/<timestamp>.png
- R2에서 수동으로 폴더를 만들 필요 없음.
